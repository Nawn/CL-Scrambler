This folder will only pull images that are of PNG extension. 

Example. Logo.png, BnWLogo.png, etc. 

If you need to convert a picture to png, 
	you can either try renaming the extension to png, or using GIMP to export the file as a PNG file.

Thank you